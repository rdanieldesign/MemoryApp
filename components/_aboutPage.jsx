var React = require('react');
var $ = require('jquery');

const AboutPage = React.createClass({
    render: function(){
        return (
            <section className="aboutPage">This is the About page.</section>
        );
    }
});

module.exports = AboutPage;